// Optional: set this to your hosted backend URL, e.g. "https://your-backend.onrender.com"
// Keep empty string to use same-origin API (works for local Express server).
window.FINNVOCAB_API_BASE = '';
